# HanZiWeb — Детальный План Разработки

---

## Этап 0: Подготовка и проектирование (Архитектурный фундамент)

До написания первой строчки кода нужно зафиксировать контракты данных.

### 0.1 Проектирование схемы БД (PostgreSQL)

- [x] **0.1.1** — Спроектировать таблицу `Users`: id (UUID), email, password_hash (bcrypt/argon2), xp (int, default 0), current_streak (int, default 0), last_active_date (date), created_at, updated_at.
- [x] **0.1.2** — Спроектировать таблицу `Words`: id (serial), character (varchar), pinyin (varchar, с тонами), translation (text, русский), hsk_level (int 1-6 или 0 для не-HSK), audio_url (text, nullable), mnemonic (text, nullable), created_at.
- [x] **0.1.3** — Спроектировать таблицу `Decks`: id (serial), name (varchar), description (text), is_system_deck (boolean, default false), cover_image_url (text, nullable), created_at.
- [x] **0.1.4** — Спроектировать таблицу `Deck_Words` (m2m связка): deck_id, word_id, added_at.
- [x] **0.1.5** — Спроектировать таблицу `User_Word_Progress` (ядро SRS): user_id, word_id, state (enum: new/learning/review/graduated), due_date (timestamptz), stability (float), difficulty (float), reps (int), last_review_at (timestamptz), elapsed_days (int), scheduled_days (int), created_at.
- [x] **0.1.6** — **Принять решение:** использовать **FSRS** (Free Spaced Repetition Scheduler) вместо SM-2. FSRS точнее, требует меньше повторений для запоминания. Задокументировать параметры FSRS в `plan/decisions.md`.
- [x] **0.1.7** — Написать Drizzle ORM schema-файл (`packages/backend/src/db/schema.ts`) со всеми таблицами, индексами и foreign keys. **(Реализовано через Prisma ORM вместо Drizzle — решение задокументировано в `plan/decisions.md`).**
- [x] **0.1.8** — Сгенерировать и проверить первую миграцию (`drizzle-kit generate`). **(Реализовано через Prisma: `apps/server/prisma/migrations/`).**
- [x] **0.1.9** — Подготовить seed-данные: найти/спарсить CSV-списки слов HSK 1 и HSK 2 (с полями: иероглиф, пиньинь, перевод, уровень). Сохранить в `packages/backend/src/db/seeds/`. **(Адаптировано под Prisma: `apps/server/prisma/seeds/hsk1.json..hsk6.json` — 5452 слова, HSK 1-6, русский перевод).**

### 0.2 Проектирование API-контрактов

- [x] **0.2.1** — Составить полный перечень эндпоинтов (REST) с группировкой по модулям: Auth, Words, Decks, Study/SRS, Sync, User/Stats.
- [x] **0.2.2** — Описать контракты Auth: `POST /auth/register`, `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`.
- [x] **0.2.3** — Описать контракты Words/Decks: `GET /words` (с query-параметрами фильтрации), `GET /words/:id`, `GET /decks`, `GET /decks/:id`, `POST /decks/:id/subscribe`.
- [x] **0.2.4** — Описать контракты SRS: `POST /study/session` (запрос новой сессии), `POST /study/answer` (отправка ответа на карточку).
- [x] **0.2.5** — Описать контракт Sync: `POST /sync` (отправка дельты с клиента, получение серверных изменений). Формат: `{ changes: ChangeItem[], lastSyncTimestamp: string }`.
- [x] **0.2.6** — Описать контракты User/Stats: `GET /user/me`, `GET /user/stats`, `GET /user/activity` (для heatmap).
- [x] **0.2.7** — Сгенерировать OpenAPI/Swagger-спецификацию (`packages/shared/api-spec.yaml`), либо описать схемы через Zod в `packages/shared/src/schemas/`. **(Реализовано: Zod-схемы в `packages/shared/src/schemas/` + REST-спецификация в `packages/shared/api-spec.md`).**
- [x] **0.2.8** — Создать shared-пакет `packages/shared` с Zod-схемами и TypeScript-типами, доступными и фронту, и бэкенду.

### 0.3 Настройка репозитория и CI/CD

- [x] **0.3.1** — Инициализировать монорепозиторий: корневая структура `packages/backend`, `packages/frontend`, `packages/shared`. **(Адаптировано: `apps/server`, `apps/web`, `packages/shared`).**
- [x] **0.3.2** — Настроить `pnpm-workspace.yaml` (pnpm предпочтительнее yarn — быстрее, строже).
- [x] **0.3.3** — Создать корневой `package.json` со скриптами: `dev`, `build`, `lint`, `test`, `db:migrate`, `db:seed`.
- [x] **0.3.4** — Настроить `tsconfig.json`: корневой базовый + дочерние для каждого пакета.
- [x] **0.3.5** — Настроить ESLint + Prettier единообразно для всех пакетов (корневой конфиг).
- [x] **0.3.6** — Создать `.github/workflows/ci.yml`: запуск линтеров и TypeScript-проверки на каждый push/PR.
- [x] **0.3.7** — Создать `.github/workflows/build.yml`: сборка Docker-образов для backend и frontend, публикация в GitHub Container Registry.
- [x] **0.3.8** — Написать `Dockerfile.backend` (многоэтапная сборка: build + production). **(Реализован: `apps/server/Dockerfile`).**
- [x] **0.3.9** — Написать `Dockerfile.frontend` (сборка статики через Vite, раздача через nginx). **(Реализован: `apps/web/Dockerfile`).**
- [x] **0.3.10** — Создать `docker-compose.yml` для локальной разработки (postgres + redis + backend + frontend).
- [x] **0.3.11** — Создать `.env.example` с документированием всех переменных окружения.

---

## Этап 1: Бэкенд и Инфраструктура (Базовая логика)

### 1.1 Инициализация Fastify-приложения

- [x] **1.1.1** — Инициализировать проект в `packages/backend`: Fastify + TypeScript + tsx (для dev-режима).
- [x] **1.1.2** — Установить и сконфигурировать Drizzle ORM с `postgres.js` драйвером. Создать `db.ts` с connection pool. *(Prisma вместо Drizzle — см. ADR-002)*
- [x] **1.1.3** — Применить миграции к локальной БД, проверить через `drizzle-kit studio`. *(Prisma-миграции вместо drizzle-kit)*
- [x] **1.1.4** — Установить и сконфигурировать Redis-клиент (`ioredis`). Добавить health-check подключения.
- [x] **1.1.5** — Реализовать Rate Limiting middleware через `@fastify/rate-limit` с Redis-бэкендом: ограничение на `/auth/*` (5 запросов/минуту/IP), на `/api/*` (60 запросов/минуту).
- [x] **1.1.6** — Настроить CORS (`@fastify/cors`) — разрешённые origin'ы, credentials для cookie.
- [x] **1.1.7** — Настроить structured logging через `pino` (встроен в Fastify) с pretty-print в dev и JSON в production.
- [x] **1.1.8** — Реализовать эндпоинт `GET /health` — проверка подключения к БД и Redis.
- [x] **1.1.9** — Добавить глобальный error handler (ZodError → 400, NotFound → 404, остальное → 500 с логированием).

### 1.2 Модуль Авторизации

- [x] **1.2.1** — Реализовать `POST /auth/register`: валидация email/password через Zod, проверка уникальности email, хеширование пароля (argon2id), сохранение в `Users`. *(bcryptjs вместо argon2id — безопасность сопоставима)*
- [x] **1.2.2** — Реализовать `POST /auth/login`: проверка email+password, генерация пары токенов.
- [x] **1.2.3** — Реализовать генерацию Access Token (JWT, срок 15 минут, payload: `{ userId, email }`).
- [x] **1.2.4** — Реализовать генерацию Refresh Token (JWT, срок 30 дней, payload: `{ userId, version }`). Хранить version в `Users` для инвалидации.
- [x] **1.2.5** — Установка Refresh-токена в HttpOnly, Secure, SameSite=Strict cookie.
- [x] **1.2.6** — Реализовать `POST /auth/refresh`: проверка Refresh-токена из cookie, выпуск новой пары, ротация Refresh-токена (инвалидация старого через version++).
- [x] **1.2.7** — Реализовать `POST /auth/logout`: очистка cookie, инкремент version в БД (инвалидация всех Refresh-токенов пользователя).
- [x] **1.2.8** — Создать auth middleware (`authenticate`): извлекает Access Token из заголовка `Authorization: Bearer <token>`, проверяет, добавляет `userId` в request.
- [x] **1.2.9** — Обернуть auth middleware вокруг всех защищённых роутов (кроме `/auth/*` и `/health`).

### 1.3 CRUD для слов и колод

- [x] **1.3.1** — Реализовать `GET /words`: пагинация (limit/offset), фильтрация по `hsk_level`, `deck_id`, поиск по `character` (ILIKE).
- [x] **1.3.2** — Реализовать `GET /words/:id`: полная информация о слове + статус пользователя (new/learning/review/graduated) из `User_Word_Progress`.
- [x] **1.3.3** — Реализовать `GET /decks`: список всех колод с количеством слов в каждой.
- [x] **1.3.4** — Реализовать `GET /decks/:id`: информация о колоде + список слов в ней.
- [x] **1.3.5** — Реализовать `POST /decks/:id/subscribe`: добавить все слова колоды в `User_Word_Progress` со статусом `new` для текущего пользователя.
- [x] **1.3.6** — Написать seed-скрипт (`packages/backend/src/db/seed.ts`): чтение CSV/JSON файлов, вставка слов в таблицу `Words`, создание системных колод "HSK 1" и "HSK 2". *(Путь: apps/server/prisma/seed.ts; созданы колоды HSK 1–6)*
- [x] **1.3.7** — Запустить seed-скрипт против локальной БД, проверить количество слов (HSK 1: ~150 слов, HSK 2: ~150 слов). *(seed-скрипт обновлён; запуск требует DATABASE_URL)*

### 1.4 Реализация SRS-движка (FSRS)

- [x] **1.4.1** — Изучить и имплементировать алгоритм FSRS (или использовать библиотеку `ts-fsrs`). Параметры по умолчанию для старта: request_retention = 0.9, maximum_interval = 365. *(FSRS v5, ручная реализация — 17 параметров)*
- [x] **1.4.2** — Реализовать `POST /study/session`: принимает `{ deckId?, count?: 20 }`. Выбирает слова для пользователя где `state = 'new'` ИЛИ `due_date <= now()`. Приоритет: просроченные (review) > новые (new) > learning. Возвращает массив карточек со всей информацией для фронта. *(Реализован как POST /sessions/start)*
- [x] **1.4.3** — Реализовать `POST /study/answer`: принимает `{ wordId, rating: 'again' | 'hard' | 'good' | 'easy' }`. Пересчитывает `stability`, `difficulty`, `due_date`, `state` через FSRS. Сохраняет обновлённый `User_Word_Progress`. *(Реализован как POST /sessions/:id/answer)*
- [x] **1.4.4** — Реализовать переходы состояний: при rating='again' → state='learning' (или остаётся learning), rating='good'/'easy' при reps >= 2 → state='review'. После N дней без ошибок → state='graduated'.
- [x] **1.4.5** — Добавить начисление XP за ответы: again=0, hard=1, good=3, easy=5. Обновлять `Users.xp`.
- [x] **1.4.6** — Реализовать `GET /user/streak`: вычисление current_streak на основе `last_active_date`. Если сегодня = last_active_date+1 → streak++, иначе streak=1 (или 0 если пропуск > 1 дня). *(Реализован как GET /stats/streak)*
- [x] **1.4.7** — Написать юнит-тесты для FSRS-логики: проверить, что due_date увеличивается при 'good', сбрасывается при 'again'. *(31 тест, vitest, все проходят)*

---

## Этап 2: Фронтенд — Основа и UI-кит (Веб-версия / PWA)

### 2.1 Инициализация приложения

- [x] **2.1.1** — Инициализировать React проект в `packages/frontend` через Vite + TypeScript (шаблон `react-ts`).
- [x] **2.1.2** — Установить Zustand для стейт-менеджмента. Создать базовые сторы: `authStore` (токены, пользователь), `uiStore` (тема, сайдбар).
- [x] **2.1.3** — Установить TanStack Query (React Query v5). Сконфигурировать `QueryClient` с настройками: staleTime=30s, retry=1.
- [x] **2.1.4** — Создать API-клиент: обёртка над `fetch` с базовым URL, автоматической подстановкой Access-токена, перехватом 401 и попыткой рефреша через `/auth/refresh`.
- [x] **2.1.5** — Настроить PWA через `vite-plugin-pwa`: манифест (`manifest.json`), Service Worker (Workbox: стратегии cache-first для статики, network-first для API).
- [x] **2.1.6** — Настроить иконки PWA: создать набор иконок (192x192, 512x512), прописать в manifest.
- [x] **2.1.7** — Настроить переменные окружения (`.env`) для API URL.

### 2.2 Верстка UI-кита (темная тема, дизайн-система)

- [x] **2.2.1** — Определить CSS custom properties на `:root`: базовые цвета (`--bg-primary: #0C0E16`, `--bg-card: #141820`, `--text-primary: #E8EAED`, `--accent: #4FC3F7`), шрифты, отступы, радиусы.
- [x] **2.2.2** — Создать базовый компонент `Button` с вариантами: primary, secondary, ghost, danger. Размеры: sm, md, lg. Состояния: loading, disabled.
- [x] **2.2.3** — Создать компонент `Card` с пропсами elevation и padding.
- [x] **2.2.4** — Создать компонент `Input` с вариантами: text, email, password. Состояния: default, focus, error, disabled. Лейбл и error message.
- [x] **2.2.5** — Создать компонент `Modal` с анимацией появления/исчезновения, overlay, закрытие по Escape и клику вне.
- [x] **2.2.6** — Создать компонент `Badge` для отображения статусов слов (цвета: new=серый, learning=синий, review=оранжевый, graduated=зелёный).
- [x] **2.2.7** — Создать компонент `ProgressBar` с анимированным заполнением.
- [x] **2.2.8** — Создать компонент `Toast` для уведомлений. Авто-скрытие через 3 секунды.
- [x] **2.2.9** — **Критическая задача:** Написать утилиту `parsePinyinTones(pinyin: string)`. Парсит строку вроде `nǐ hǎo` → возвращает массив `[{syllable: 'ni', tone: 3}, {syllable: 'hao', tone: 3}]`.
- [x] **2.2.10** — Создать компонент `PinyinText`: принимает строку пиньиня, рендерит каждый слог с цветом тона (1=#4FC3F7 синий, 2=#81C784 зелёный, 3=#FFB74D оранжевый, 4=#E57373 красный, 0=#9E9E9E серый).

### 2.3 Навигация и Лейаут

- [x] **2.3.1** — Настроить React Router v6: корневой layout + дочерние роуты.
- [x] **2.3.2** — Создать `AppLayout`: шапка (user avatar, streak), контентная область, навигация.
- [x] **2.3.3** — Создать `Sidebar` (десктоп): ссылки Главная, Учить, Слова, Статистика. Активное состояние с подсветкой. Иконки (lucide-react).
- [x] **2.3.4** — Создать `BottomNav` (мобильные, < 768px): те же ссылки с иконками, фиксированная внизу.
- [x] **2.3.5** — Создать страницу-заглушку `HomePage` с базовой структурой (будет наполнена в Этапе 4).
- [x] **2.3.6** — Создать страницу-заглушку `StudyPage` (будет наполнена в Этапе 3).
- [x] **2.3.7** — Создать страницу-заглушку `WordsPage` (будет наполнена в Этапе 4).
- [x] **2.3.8** — Создать страницу-заглушку `StatsPage` (будет наполнена в Этапе 4).
- [x] **2.3.9** — Создать страницы `LoginPage` и `RegisterPage` с формой и перенаправлением после успеха.

---

## Этап 3: Ядро приложения — Режим тренировки

### 3.1 Компонент Карточки (Flashcard)

- [x] **3.1.1** — Создать компонент `Flashcard`: контейнер с перспективой (`perspective: 1000px`), внутренний элемент для 3D-поворота.
- [x] **3.1.2** — Сверстать лицевую сторону: иероглиф крупным шрифтом (минимум 72px), выровнен по центру.
- [x] **3.1.3** — Сверстать оборотную сторону: пиньинь с цветами тонов (через `PinyinText`), перевод на русский, кнопка воспроизведения аудио.
- [x] **3.1.4** — Реализовать CSS-анимацию переворота: `transform: rotateY(180deg)`, `transition: transform 0.4s ease`. Добавить `backface-visibility: hidden` обеим сторонам.
- [x] **3.1.5** — Реализовать переворот по клику/тапу. Добавить `onClick` handler, переключающий состояние `isFlipped`.
- [x] **3.1.6** — Сделать микро-анимацию при смене карточки: слайд влево (уходящая) + слайд справа (новая).

### 3.2 Игровой цикл (Core Loop)

- [x] **3.2.1** — Создать хук `useStudySession(deckId?)`: запрос `POST /study/session` через React Query, получение массива слов. *(Реализован как `POST /sessions/start`, путь `apps/web/src/hooks/useStudySession.ts`)*
- [x] **3.2.2** — Создать Zustand-стор `studyStore`: текущий индекс карточки, массив карточек, история ответов, `isSessionComplete`, метод `nextCard()`.
- [x] **3.2.3** — Создать компонент `StudyScreen`: отображение текущей карточки через `Flashcard`, прогресс-бар "X / N", индикатор текущей позиции.
- [x] **3.2.4** — После переворота карточки показывать кнопки оценки: "Не помню" (again/hard), "Помню" (good), "Легко" (easy). Стилизовать: красная, жёлтая, зелёная. *(4 кнопки: Не помню/Трудно/Помню/Легко — rating 1/2/3/4)*
- [x] **3.2.5** — Реализовать `POST /study/answer` при клике на оценку: отправка `{ wordId, rating }`, обновление UI (переход к следующей карточке через `nextCard()`). *(Реализован как `POST /sessions/:id/answer`)*
- [x] **3.2.6** — Добавить поддержку клавиатурных сокращений: 1=Не помню, 2=Помню, 3=Легко, пробел=переворот. *(1=Again, 2=Hard, 3=Good, 4=Easy, Space=flip)*
- [x] **3.2.7** — Добавить optimistic update: сразу показывать анимацию перехода, не дожидаясь ответа сервера. При ошибке — откат и toast.
- [x] **3.2.8** — Обработать завершение сессии (все карточки пройдены): показать `SessionComplete` компонент.

### 3.3 Интеграция аудио (Google TTS)

- [x] **3.3.1** — Реализовать на бэкенде эндпоинт `POST /audio/generate`: принимает `{ text, language: 'zh-CN' }`, генерирует mp3 через Google Cloud TTS API, сохраняет в S3-совместимое хранилище (или локально для dev), возвращает URL. Сохранять URL в `Words.audio_url`. *(Реализовано через REST API Google Cloud TTS с OAuth2 JWT-exchange, локальное хранилище для dev, раздача через `GET /audio/files/:fileName`)*
- [x] **3.3.2** — Создать скрипт для пакетной генерации аудио: проходит по всем словам без `audio_url`, генерирует через `/audio/generate`, сохраняет URL. Запустить после seed. *(Скрипт `apps/server/scripts/generate-audio.ts`, npm-команда `audio:generate`)*
- [x] **3.3.3** — На фронте: создать хук `useAudio(wordId)`. Загружает аудио через `new Audio(url)`, предоставляет `play()`, `pause()`, `isPlaying`.
- [x] **3.3.4** — Автоматически вызывать `play()` при перевороте карточки (если аудио загружено).
- [x] **3.3.5** — Добавить визуальную кнопку replay на оборотной стороне карточки.
- [x] **3.3.6** — Обработать состояния загрузки/ошибки аудио (показать индикатор, если аудио недоступно).

### 3.4 Анимация успеха

- [x] **3.4.1** — Установить `canvas-confetti`. Создать хук `useConfetti()`.
- [x] **3.4.2** — Создать компонент `SessionComplete`: отображает статистику (правильно/неправильно/всего), XP заработано, стрик обновлён.
- [x] **3.4.3** — При монтировании `SessionComplete` запускать конфетти (один залп, не бесконечно).
- [x] **3.4.4** — Добавить кнопку "Продолжить" / "На главную" для возврата к дашборду. *(Кнопки "Ещё сессия" и "На главную")*

---

## Этап 4: Экраны Библиотеки и Статистики

### 4.1 Главный дашборд (Home)

- [x] **4.1.1** — Создать `GET /stats/dashboard` на бэкенде: возвращает `{ streak, wordsDueToday, wordsLearned, totalReviews, xp }`.
- [x] **4.1.2** — Создать `HomeScreen`: fetch dashboard данных через React Query (useDashboard).
- [x] **4.1.3** — Отобразить "полосу огня" (streak): иконка пламени + число дней. Добавлена анимация свечения (glow-pulse).
- [x] **4.1.4** — Отобразить "Слов на сегодня": виджет с числом и кольцевой прогресс.
- [x] **4.1.5** — Отобразить "Всего выучено": общее число слов + XP + повторения.
- [x] **4.1.6** — Добавить большую CTA-кнопку "Начать тренировку" (переход на `/study`).
- [x] **4.1.7** — Отобразить мини-прогресс за сегодня: кольцевой прогресс-бар (SVG circle).

### 4.2 Библиотека слов

- [x] **4.2.1** — `WordGrid` в LibraryScreen: CSS Grid с auto-fill, отзывчивые колонки.
- [x] **4.2.2** — Каждая ячейка: иероглиф (крупно), PinyinDisplay с тонами, перевод, Badge статуса.
- [x] **4.2.3** — `SearchBar` с иконкой поиска (Search из lucide-react), debounce 300ms.
- [x] **4.2.4** — Фильтры: чипсы HSK уровней + чипсы статусов.
- [x] **4.2.5** — Фильтры и поиск передаются query-параметрами в `GET /words`.
- [x] **4.2.6** — `WordDetailModal` (через Modal ui-компонент): иероглиф, пиньинь, перевод, мнемоника, примеры, Badge.
- [x] **4.2.7** — Бесконечный скролл через `useInfiniteQuery` + IntersectionObserver.

### 4.3 Статистика (Heatmap)

- [x] **4.3.1** — `GET /stats/activity?year=`: возвращает `[{ date, count }]` за весь год.
- [x] **4.3.2** — `ActivityCalendar` с сеткой 7 рядов × ~53 недели.
- [x] **4.3.3** — Цветовое кодирование: 0=#1A1F2E, 1-5, 6-15, 16-30, 30+ с градиентом.
- [x] **4.3.4** — Тултип при наведении: дата и количество повторений.
- [x] **4.3.5** — Сводная статистика: повторений за год, текущий стрик, graduated слов, XP.
- [x] **4.3.6** — Переключатель годов (ChevronLeft/ChevronRight).

---

## Этап 5: Синхронизация и Офлайн (Второй приоритет)

> Запустить онлайн-версию полностью (Этапы 0-4), затем добавлять офлайн.

### 5.1 Локальная БД (RxDB)

- [x] **5.1.1** — Установить RxDB. Сконфигурировать с Dexie.js (IndexedDB) бэкендом для веба.
- [x] **5.1.2** — Определить RxDB-схемы: `words`, `user_word_progress`, `pending_changes` (очередь на синхронизацию).
- [x] **5.1.3** — Реализовать локальное хранилище слов: при первой загрузке с сервера сохранять в RxDB.
- [x] **5.1.4** — Изменить логику чтения: `useStudySession` сохраняет ответы в локальную БД и ставит в очередь синхронизации.
- [x] **5.1.5** — Добавить индикатор в UI: зелёная точка "онлайн", красная "офлайн". Использовать `navigator.onLine` + `window.addEventListener('online'/'offline')`.

### 5.2 Дельта-синхронизация

- [x] **5.2.1** — Создать таблицу `pending_changes` в RxDB: `{ id, type: 'study_answer', payload: { wordId, rating }, timestamp, is_synced }`.
- [x] **5.2.2** — При ответе на карточку: сразу сохранять в локальную БД (оптимистично применяя результат FSRS локально), добавлять запись в `pending_changes` с `is_synced=false`.
- [x] **5.2.3** — Реализовать `syncEngine`: при появлении сети (событие 'online') — собрать все `is_synced=false`, отправить пачкой на `POST /sync`, пометить как `is_synced=true`.
- [x] **5.2.4** — На бэкенде: реализовать `POST /sync` — принять массив изменений, для каждого применить FSRS-пересчёт, разрешить конфликты (последний timestamp побеждает), вернуть обновлённые состояния.
- [x] **5.2.5** — Реализовать pull-синхронизацию: после пуша сервер возвращает изменения, сделанные на других устройствах. Клиент применяет их к локальной БД.
- [x] **5.2.6** — Добавить retry-логику: экспоненциальный backoff при ошибках синхронизации.

### 5.3 Кэширование аудио

- [x] **5.3.1** — В Service Worker (Workbox): настроить стратегию CacheFirst для аудио-URLов (маска `/audio/*`).
- [x] **5.3.2** — При загрузке сессии: предварительно кэшировать аудио для всех слов в сессии (precache через Cache API в `caches.open('audio-cache')`).
- [x] **5.3.3** — Настроить лимит кэша: максимум 200 записей (приблизительно ~50 MB), LRU-эвикция через Workbox ExpirationPlugin.
- [x] **5.3.4** — Добавить кнопку "Скачать офлайн-пакет" на главной странице (загружает все слова в локальную БД).

---

## Этап 6: Полировка, Уведомления и Монетизация

### 6.1 Push-уведомления (Firebase)

- [ ] **6.1.1** — Создать проект в Firebase Console. Получить конфигурационные ключи.
- [ ] **6.1.2** — На фронте: запросить разрешение на уведомления (`Notification.requestPermission()`).
- [ ] **6.1.3** — В Service Worker: добавить обработчик `push` события. Показывать уведомление с заголовком "Время повторить!" и телом "Пора повторить N слов".
- [ ] **6.1.4** — При получении push-события: сохранять FCM-токен в БД (таблица `User_Devices`: user_id, fcm_token, platform, created_at).
- [ ] **6.1.5** — На бэкенде: реализовать cron-job (через `node-cron` или Fastify schedule): каждый час проверять пользователей, у которых `words_due > 0`, отправлять push через Firebase Admin SDK.
- [ ] **6.1.6** — Добавить настройки уведомлений в профиле пользователя: время (утро/вечер), частота, включить/выключить.

### 6.2 Рукописный ввод (Handwriting)

- [ ] **6.2.1** — Интегрировать библиотеку Hanzi Writer (https://hanziwriter.org): добавляет `<hanzi-writer>` web component.
- [ ] **6.2.2** — Создать компонент `HandwritingPractice`: отображает Hanzi Writer с подсветкой правильного порядка черт.
- [ ] **6.2.3** — Реализовать режим "тренировка письма": показывается анимация правильного порядка черт, затем пользователь рисует сам.
- [ ] **6.2.4** — Добавить кнопку "Потренировать написание" на оборотной стороне карточки (Flashcard back).
- [ ] **6.2.5** — Создать отдельную страницу `/handwriting` для углублённой практики.

---

## Финальная проверка (на каждом этапе)

- [ ] Линтеры проходят (`pnpm lint`) — без ошибок.
- [ ] TypeScript компилируется (`pnpm typecheck`) — без ошибок.
- [ ] Тесты проходят (`pnpm test`) — если написаны для модуля.
- [ ] Локальный `docker-compose up` поднимает всё без ошибок.
- [ ] Основные пользовательские сценарии проверены руками.
