# Dev override для API base URL (PLAN_Features_v0.4 §54).
# Expo автоматически подставляет переменные с префиксом EXPO_PUBLIC_
# на старте dev-сервера и при сборке. Без этого файла используется
# `app.json#extra.apiUrl` (prod-дефолт).
EXPO_PUBLIC_API_URL=http://localhost:3001/api
